<div id="layout" class="pure-g">
  <div class="sidebar pure-u-1-4">
    <div class="header">
      <hgroup>
        <h1 class="brand-title">{{t 'blog.title'}}</h1>
        <h4 class="brand-tagline">{{t 'blog.description'}}</h4>
      </hgroup>

      <nav class="nav">
        <ul class="nav-list">
          <li class="nav-item">{{#link-to 'index' class="link-button"}}{{t 'blog.home'}}{{/link-to}}</li>
          <li class="nav-item">{{#link-to 'index' class="link-button"}}{{t 'blog.posts'}}{{/link-to}}</li>
          {{#if currentUser}}
            <li class="nav-item">{{#link-to 'settings' class="link-button"}}{{t 'blog.admin'}}{{/link-to}}</li>
          {{/if}}
          <li class="nav-item">
            {{#if currentUser}}
              <a class="link-button" {{action 'logout'}}>{{t 'button.logout'}}</a>
            {{else}}
              <a class="link-button" {{action 'showLogin'}}>{{t 'button.login'}}</a>
            {{/if}}
          </li>
        </ul>
      </nav>
    </div>
  </div>

  <div class="content pure-u-3-4">
    {{outlet}}
  </div>
</div>

{{outlet 'modal'}}

{{alert-modal content=alertContent}}
